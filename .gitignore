__pycache__/
*.pyc
experiments/pilot.log
experiments/*.log
experiments/activations_l6.pt
experiments/token_data.json
experiments/gpt2_encoder.json
results/real/*.pt
results/real/acts_*
.venv/
results/real/sae_pythia-70m_*.json

# Derived artifact: rebuild with ops/build_paper.sh (avoids binary churn every round).
PAPER.pdf

# Same rule for the figures: figs/*.tex is tracked and regenerates from the frozen
# result JSON via ops/mkfigs.py, so the compiled PDFs are derived, not committed.
figs/*.pdf
figs/*.aux
figs/*.log
results/real/r15_words/
